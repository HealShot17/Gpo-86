Estructuras de control
Palabras reservadas
Variables y constantes
    #Ejepmlo para congelar variables: 

        PI = 3.1415926535


        #Comando para congelar un dato para que pase de variable a constante 

        data = (31415926535,10)
         #PI  =frozenset(data)

        #Generar diccionario

        valores={'pi': 31415926535}
        print( valores['pi'])
    Definicion de variables
        Entero = int(input())
        flotante= float(input())
        pinocho= str(input())
Tipos de datos diferenciados
Adquirir y mostrar datos 

Buenas practicas en la programacion 
    a='x'
    b= "equis we"
    #Si solo se caputra una letra se ponen comillas simples si son mas de 1 se ponen comillas dobles 



listas
    lista1 = list()
    lista2 = []
for i in range (8):
    lista1.append(input(f"Ingrese el lugar {i+1}))
lista1.remove("6")
print(lista1.index("luis"))

a = int(input("Ingrese las filas: "))
b = int(input("Ingrese las columnas: "))

for i in range(a)
    lista2.append(int(input(f"Ingrese el elemento de la fila {i} y la columnas {j})))



Python es un programa ejecutable